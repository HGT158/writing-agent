# 后续待办

本文件只记录已经确认但不属于当前完成范围的能力。实施任一事项前，必须重新设计、更新架构单一事实来源并按 TDD 完成回归。

## 长会话上下文压缩

状态：暂缓。

当前项目 Agent 会把所选会话的全部可见 user/assistant 历史按顺序传给模型，不做静默截断。随着会话增长，后续需按 token 预算压缩较早消息，同时保留最近原始消息、未处理 diff 所需上下文和可审计的摘要来源。

实施时至少覆盖：

- 压缩触发阈值与模型 token 预算。
- 摘要的持久化、版本和重新生成策略。
- 最近原始消息与 pending change set 上下文的强制保留。
- 助手、项目、会话三层隔离和失败降级。
- 摘要前后模型输入可追溯，禁止无提示丢失历史。
